import React from 'react';

function TaskList() {
  // O 'div' é só para o componente não ficar vazio
  return <div>TaskList Component</div>;
}

// A linha mais importante! Ela permite que o componente seja importado.
export default TaskList;